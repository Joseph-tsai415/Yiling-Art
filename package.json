{
  "name": "yiling-art",
  "version": "0.0.0",
  "private": true,
  "description": "Static, zero-build browser tools for designers, served from one repo.",
  "scripts": {
    "start": "http-server . -p 8000 -c-1 -o /",
    "serve": "http-server . -p 8000 -c-1",
    "gen:schema": "node bakery-erp/tools/gen-schema.mjs",
    "check:schema": "node bakery-erp/tools/gen-schema.mjs --check"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "whatwg-encoding": "^3.1.1"
  }
}
