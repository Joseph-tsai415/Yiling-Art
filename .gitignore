# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
.claude/
*.swp
*.swo

# Node (if added later)
node_modules/
dist/

# Environment
.env
.env.local

# Local-only Google API credentials (injected from GitHub secrets at deploy)
product-card-printer/google-config.local.js

# Local-only scratch assets (sample images, screenshots) and any credentials
# files dropped here — never commit (contains OAuth client_secret).
product-card-printer/assets/
